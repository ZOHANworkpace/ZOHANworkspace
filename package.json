{
  "name": "zohanworkspace-server",
  "version": "2.0.0",
  "description": "ZOHANWORKSPACE — backend: auth, per-user cloud state, and a server-side Gemini proxy (no more API keys typed into prompt() boxes).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.7.0"
  }
}
